import { StyleSheet } from "react-native";

export const stylesGlobal = StyleSheet.create({
  image: {
    width: 250,
    height: 250,
  },
  container: {
    height: "100%",
    flex: 1,
    alignItems: "center",
  },
});
